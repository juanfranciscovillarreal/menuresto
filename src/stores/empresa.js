import { defineStore } from 'pinia'

export const useEmpresaStore = defineStore('empresa', () => {
    // Estados
    const empresa = ref({})

    // Getters

    // Actions

    return {
        empresa,
    }
})